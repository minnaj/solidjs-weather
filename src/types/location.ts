export type LocationResult = {
  id: string;
  country: string;
  lat: number;
  lon: number;
  name: string;
  region: string;
  url: string;
};

export type Position = {
  latitude: number;
  longitude: number;
};
